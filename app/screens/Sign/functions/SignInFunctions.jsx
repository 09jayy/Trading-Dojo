export const signIn = (username, password, setter) => {
    console.log("username: ", username); // replace with sign in functionality when the time comes
    console.log("password: ", password);
    setter(true);
}